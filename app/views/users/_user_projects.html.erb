<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= fields_for :projects do |project_field| %>
    <%= project_field.label :project_id %><br />
    <%= project_field.text_field :project_id %>
  </div>

  <div class="actions">
    <%= user.submit %>
  </div>
<% end %>